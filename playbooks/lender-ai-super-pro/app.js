const spreads=[...document.querySelectorAll('.spread')];const PREFIX='ocg-lender-book-';let current=Number(localStorage.getItem(PREFIX+'spread')||0);if(current<0||current>=spreads.length)current=0;let guideMode='guide';const chapterList=document.getElementById('chapterList');spreads.forEach((s,i)=>{const b=document.createElement('button');b.innerHTML=`<span>${s.dataset.chapter}</span><span>${s.dataset.title}</span><span class="chapter-status">•</span>`;b.onclick=()=>goTo(i);b.dataset.index=i;chapterList.appendChild(b)});function render(dir=0){spreads.forEach((s,i)=>{s.classList.toggle('active',i===current);if(i===current&&dir){s.querySelectorAll('.page').forEach(p=>{p.classList.remove('turn-next','turn-prev');void p.offsetWidth;p.classList.add(dir>0?'turn-next':'turn-prev')})}});document.querySelectorAll('.chapter-list button').forEach(b=>b.classList.toggle('active',Number(b.dataset.index)===current));document.getElementById('prevBtn').disabled=current===0;document.getElementById('nextBtn').disabled=current===spreads.length-1;document.getElementById('spreadCounter').textContent=`Spread ${current+1} of ${spreads.length}`;localStorage.setItem(PREFIX+'spread',current);markRead(current);updateAssistantContext();window.scrollTo({top:0,behavior:'smooth'})}function nextSpread(){if(current<spreads.length-1){current++;render(1)}}function prevSpread(){if(current>0){current--;render(-1)}}function goTo(i){const dir=i>current?1:-1;current=i;render(dir)}function markRead(i){let read=JSON.parse(localStorage.getItem(PREFIX+'read')||'[]');if(!read.includes(i)){read.push(i);localStorage.setItem(PREFIX+'read',JSON.stringify(read))}updateProgress()}function updateProgress(){const read=new Set(JSON.parse(localStorage.getItem(PREFIX+'read')||'[]'));const pct=Math.round(read.size/spreads.length*100);document.getElementById('progressFill').style.width=pct+'%';document.getElementById('progressText').textContent=pct+'% complete';document.getElementById('pagesReadText').textContent=`${read.size} / ${spreads.length}`;document.querySelectorAll('.chapter-list button').forEach(b=>{const done=read.has(Number(b.dataset.index));b.classList.toggle('complete',done);const s=b.querySelector('.chapter-status');if(s)s.textContent=done?'✓':'•'});document.getElementById('assignmentsText').textContent=`${pct>=50?1:0} / 2`;if(pct>=50){document.getElementById('assignment-daily').classList.add('complete');document.querySelector('#assignment-daily .assignment-check').textContent='✓'}if(pct===100){document.getElementById('assignment-plan').classList.add('complete');document.querySelector('#assignment-plan .assignment-check').textContent='✓'}}function toggleTheme(){document.body.classList.toggle('dark');localStorage.setItem(PREFIX+'theme',document.body.classList.contains('dark')?'dark':'light')}if(localStorage.getItem(PREFIX+'theme')==='dark')document.body.classList.add('dark');async function copyText(id,btn){try{await navigator.clipboard.writeText(document.getElementById(id).innerText);const old=btn.textContent;btn.textContent='Copied';setTimeout(()=>btn.textContent=old,1200)}catch{btn.textContent='Select + Copy'}}function openAssistant(){document.getElementById('assistant').classList.add('open');updateAssistantContext()}function closeAssistant(){document.getElementById('assistant').classList.remove('open')}function setGuideMode(mode,btn){guideMode=mode;document.querySelectorAll('.role-tabs button').forEach(x=>x.classList.remove('active'));if(btn)btn.classList.add('active');const hints={guide:'Ask Harper to explain the current lesson or help apply it.',example:'Describe what you want to see as a realistic lender example.',roleplay:'Tell Harper who to play: borrower, Realtor, past client, or video viewer.',prompt:'Describe the business problem and Harper will identify the best prompt/workflow.'};document.getElementById('askInput').placeholder=hints[mode]}function updateAssistantContext(){const s=spreads[current];const el=document.getElementById('assistantContext');if(el)el.textContent=`Reading: ${s?.dataset.chapter||''} · ${s?.dataset.title||''}`}function answerQuestion(){const input=document.getElementById('askInput'),q=input.value.trim(),s=spreads[current],title=s?.dataset.title||'this lesson',out=document.getElementById('assistantAnswer');let answer='';if(guideMode==='roleplay'){answer=`<div class="answer-label">Harper · Role-play</div><p><b>I’ll play the other person.</b></p><p>${roleOpening(q,title)}</p><p><b>Your turn:</b> answer naturally. Keep it conversational. After you respond, ask me to challenge the answer or coach it.</p>`}else if(guideMode==='prompt'){answer=`<div class="answer-label">Harper · Find Prompt</div><p>${promptFinder(q)}</p><p>I’ll keep borrower-specific numbers, current rates, eligibility and program claims behind a verification gate.</p>`}else if(guideMode==='example'){answer=`<div class="answer-label">Harper · Example</div><p>${exampleFor(title,q)}</p>`}else{answer=`<div class="answer-label">Harper · Guide</div><p>${guideFor(title,q)}</p>`}out.innerHTML=answer;input.value=''}function roleOpening(q,title){const t=(q+' '+title).toLowerCase();if(t.includes('realtor'))return '“Thanks for reaching out, Brandy. I already have a lender I use regularly. What would be different about working with you?”';if(t.includes('rate'))return '“I think I should wait. Rates feel too high and I don’t want to buy at the wrong time.”';if(t.includes('cash')||t.includes('closing'))return '“The monthly payment isn’t my only concern. I’m worried about how much cash I’ll actually need at closing.”';if(t.includes('video'))return '“I’m the viewer. Give me the first 15 seconds of your video without sounding like you’re reading a mortgage ad.”';return '“I’m interested in buying, but I’m nervous about talking to a lender because I don’t know whether I’m ready yet.”'}function promptFinder(q){const t=q.toLowerCase();if(t.includes('realtor'))return '<b>Use: Realtor Partner Touch.</b> Give Harper the relationship context, what value you can genuinely offer, and the desired next step. Avoid referral compensation language.';if(t.includes('ghost')||t.includes('no reply'))return '<b>Use: No-Response Follow-Up.</b> Paste the last conversation and current stage. The goal is a helpful, low-pressure next step.';if(t.includes('video')||t.includes('post')||t.includes('content'))return '<b>Use: Content Scout → Script Studio.</b> Identify the audience and topic, verify current claims, approve the script, then choose self-record or avatar.';if(t.includes('objection')||t.includes('rate')||t.includes('payment'))return '<b>Use: Objection Coach.</b> Paste exactly what the person said and separate the concern from any numeric scenario that requires current verified data.';return '<b>Start with the Morning Business Brief or Appointment Prep.</b> Paste the raw situation; Harper can narrow it to the right workflow without requiring you to rewrite everything.'}function exampleFor(title,q){if(title.includes('Content'))return 'Example: Harper identifies a first-time-buyer misconception worth explaining. The script stays general until any current program facts are sourced. Brandy approves it, then records herself or uses her consented avatar. QA checks the spoken claims, captions, NMLS/company disclosures and CTA before the publish package is prepared.';return `For ${title}, start with the real situation exactly as it happened. Harper separates known facts from missing information, prepares the smallest useful next step, and flags anything that needs current mortgage or company verification before it becomes client-facing.`}function guideFor(title,q){const map={'The Operating System':'The system is designed around five jobs: research, educate, follow up, create and review. AI handles preparation and consistency; Brandy keeps professional judgment and approval.','Meet Harper':'Harper is the playbook specialist. Use Guide to understand a lesson, Example to see it applied, Role-play to practice conversations, and Find Prompt when you know the problem but not the tool.','Today’s AI Routine':'The daily routine should reduce decisions: surface priorities, prepare communications, identify one relationship touch and recommend one useful content opportunity.','Content + Video':'The video workflow begins before the camera. Scout the topic, verify current claims, approve the script, choose self-record or avatar, then run independent QA.','Prompt Vault':'The Prompt Vault prevents blank-page work. Describe the real situation and use a prepared lender-safe workflow instead of inventing a prompt from scratch.','30-Day Plan':'Do not connect everything on day one. First prove the daily brief, follow-up, coaching and video routines are useful; then authorize the integrations that remove repeated manual work.'};return map[title]||`This chapter is about ${title}. Tell me what part you want to apply and I’ll walk you through it without inventing borrower or program facts.`}function toggleVoice(){const input=document.getElementById('askInput');const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR){input.placeholder='Voice input is not supported in this browser. Type your question here.';return}const r=new SR();r.onresult=e=>input.value=e.results[0][0].transcript;r.start()}function stopSpeaking(){if('speechSynthesis'in window)speechSynthesis.cancel()}render();